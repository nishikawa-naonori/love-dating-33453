  <% if user_signed_in? %>

      <% if romance.like_user(current_user.id) %>
        <%= button_to romance_like_path(like, romance_id: romance.id), method: :delete, id: "like-button", remote: true do %>
        <%# <%= image_tag("icon_red_heart.svg") %> %>
        <span>
          <%= romance.like_count %>
        </span>
      <% end %>

  <% else %>
    <%= button_to romance_likes_path(romance), id: "like-button", remote: true do %>
      <%# <%= image_tag("icon_heart.svg") %> %>
      <span>
        <%= romance.like_count %>
      </span>
  <% end %>
  
  <% end %>
<% else %>
  <%# <% = image_tag("icon_heart.svg") %> %>
    <span>
      <%= romance.like_count %>
    </span>
<% end %>